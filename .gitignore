# local env / secrets (config.js holds public anon key only, but keep personal overrides out)
config.local.js

# OMC / agent state (should never be committed)
.omc/

# local repro/debug scripts
repro.mjs
repro2.mjs
verify.mjs
verify.png

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# node (if added later)
node_modules/